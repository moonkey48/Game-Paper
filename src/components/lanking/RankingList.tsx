import React from 'react';

/**
 * {[userName:string]:value}를 받아서
 * payload에 있는 값을 비교해서 가장 높은 값부터 낮은 순으로 배열을 전달해주는
 */
const RankingList = () => {
    return <h1>ranking list</h1>
}

export default RankingList;